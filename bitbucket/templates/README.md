# Bitbucket Pipelines Templates

Future: Bitbucket Pipelines implementation of `common/methodology/`.

This directory will contain platform-specific templates that conform to the
shared methodology specs defined in `common/methodology/`. The same validation
approach used for GitLab (`common/tests/validate-gitlab.sh`) will be mirrored
here once Bitbucket templates are authored.
