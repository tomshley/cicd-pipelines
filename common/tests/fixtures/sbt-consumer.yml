include:
  - local: /gitlab/templates/.stages-base.yml
  - local: /gitlab/templates/.gitflow-base.yml
  - local: /gitlab/templates/.gitflow-jobs.yml
  - local: /gitlab/templates/.artifact-publish-policy.yml

compile:
  stage: build
  image: alpine
  script: ["echo test"]
